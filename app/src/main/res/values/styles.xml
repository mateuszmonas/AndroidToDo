<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="android:typeface">monospace</item>
    </style>

    <style name="Layout" parent="AppTheme">
        <item name="android:background">@color/colorBackground</item>
    </style>

    <style name="ListItem" parent="Layout">
        <item name="android:padding">12dp</item>
    </style>

    <style name="Text" parent="AppTheme">
        <item name="android:textSize">18sp</item>
        <item name="android:typeface">monospace</item>
    </style>

    <style name="WidgetText" parent="AppTheme">
        <item name="android:textSize">15sp</item>
        <item name="android:textColor">#000</item>
        <item name="android:typeface">monospace</item>
    </style>

    <style name="WidgetTitleText" parent="WidgetText">
        <item name="android:textSize">18sp</item>
    </style>

    <style name="TitleText" parent="Text">
        <item name="android:textSize">20sp</item>
    </style>

    <style name="SplashTheme" parent="Theme.AppCompat.NoActionBar">
        <item name="android:windowBackground">@drawable/splash</item>
    </style>

</resources>
